// stderr notes that earned their keep.
//
// The state that stops them repeating every pass moved to
// RecognitionSession — it is per-run, not global.
//
// Split from the original single-file OCR helper; see docs/REFACTOR.md.

import Foundation
